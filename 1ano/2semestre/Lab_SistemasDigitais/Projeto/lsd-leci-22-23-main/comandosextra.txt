--- port adicionar --time_extra : in std_logic_vector(7 downto 0);



	states adiconar EXTRA
E a varialvel time_extra no process

when EXTRA =>
                LEDG <= "001"; -- LEDG(0) ON significa que ainda esta a cozer mais um pouco como tempo extra
                timer_enable <= '1';
                counter <= time_extra;
                TimeValue <= counter;
                display_time <= counter; -- display individual time
                if timer_exp = '1' then
                    next_state <= IDLE;
                else
                    next_state <= EXTRA;
                end if;


situaçao do extra com as key's
